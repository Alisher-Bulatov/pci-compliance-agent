version: 1
applications:
  - appRoot: webui   # must match AMPLIFY_MONOREPO_APP_ROOT
    frontend:
      phases:
        preBuild:
          commands:
            - npm ci
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: dist
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
